# Learn more about services, parameters and containers at
# https://symfony.com/doc/current/service_container.html
services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: false

    # makes classes in src/AppBundle available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    AppBundle\:
        resource: '../../src/AppBundle/*'
        # you can exclude directories or files
        # but if a service is unused, it's removed anyway
        exclude: '../../src/AppBundle/{Entity,Repository,Tests,Legacy,Bioapi}'

    MinitoolsBundle\:
        resource: '../../src/MinitoolsBundle/*'
        # you can exclude directories or files
        # but if a service is unused, it's removed anyway
        exclude: '../../src/MinitoolsBundle/{Entity,Repository,Tests,Legacy}'

    # controllers are imported separately to make sure they're public
    # and have a tag that allows actions to type-hint services
    AppBundle\Controller\:
        resource: '../../src/AppBundle/Controller'
        public: true
        tags: ['controller.service_arguments']

    MinitoolsBundle\Controller\:
        resource: '../../src/MinitoolsBundle/Controller'
        public: true
        tags: ['controller.service_arguments']

    AppBundle\Bioapi\Bioapi:
        arguments:
            - '@csa_guzzle.client.bioapi'
            - '@jms_serializer'

    AppBundle\Service\ProteinManager:
        arguments:
            - "%wts%"

    AppBundle\Service\SequenceManager:
        arguments:
            - "%dna_complements%"
            - "%rna_complements%"
            - "%elements%"
            - "%chemical_groups%"
            - "%codons%"

    AppBundle\Service\SeqAlignManager:
        arguments:
            - "%alphabet%"

    AppBundle\Twig\AppExtension:
        arguments:
            - '%protein_colors%'
            - '%generic_colors%'

    MinitoolsBundle\Service\DnaToProteinManager:
        arguments:
            - "%codons%"
            - "%triplets%"
            - "%triplets_combinations%"

    MinitoolsBundle\Service\ChaosGameRepresentationManager:
        arguments:
            - '%nucleotids_graphs%' # keep it

    MinitoolsBundle\Service\RandomSequencesManager:
        arguments:
            - '%dna_complements%'
            - '%codons%'

    MinitoolsBundle\Service\ReduceProteinAlphabetManager:
        arguments:
            - '%protein_colors%'
            - '%reductions%'

    MinitoolsBundle\Service\RestrictionDigestManager:
        arguments:
            - '%vendor_links%'

    MinitoolsBundle\Form\RestrictionEnzymeDigestType:
        arguments:
            - '%vendors%'

    MinitoolsBundle\Service\SequenceAlignmentManager:
        arguments:
            - '%pam250_matrix%'